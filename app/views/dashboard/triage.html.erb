<div class="dashboard-sidebar">
  <div class="sidebar-stats">
    <div class="stat-card">
      <div class="stat-label">Total Waiting</div>
      <div class="stat-value"><%= @total_waiting %></div>
    </div>
    
    <div class="stat-card">
      <div class="stat-label">Avg Wait Time</div>
      <div class="stat-value"><%= @avg_wait_time.round %>m</div>
    </div>
    
    <div class="stat-card">
      <div class="stat-label">RP Utilization</div>
      <div class="stat-value"><%= @rp_utilization %>%</div>
    </div>
  </div>
</div>

<div class="dashboard-main">
  <h2>Waiting Patients</h2>
  
  <div class="patient-table">
    <table>
    <thead>
      <tr>
        <th>Patient</th>
        <th>Care Pathway</th>
        <th>ESI</th>
        <th>Wait Progress</th>
        <th>Chief Complaint</th>
        <th>RP Eligible</th>
      </tr>
    </thead>
    <tbody>
      <% @patients.each do |patient| %>
        <tr data-patient-id="<%= patient.id %>">
          <td>
            <%= link_to patient_path(patient), data: { turbo_frame: "patient_modal" } do %>
              <div><strong><%= patient.full_name %></strong></div>
              <div style="font-size: 12px; color: #586069;"><%= patient.age %>Y • MRN: <%= patient.mrn %></div>
            <% end %>
          </td>
          <td>
            <div class="care-pathway-bar">
              <%= link_to patient_care_pathways_path(patient, referrer: 'triage'), 
                          class: "btn-action" do %>
                <%= patient.active_care_pathway&.progress_percentage || 0 %>%
              <% end %>
            </div>
          </td>
          <td>
            <span class="esi-badge esi-<%= patient.esi_level %>">
              ESI <%= patient.esi_level %>
            </span>
          </td>
          <td>
            <div class="wait-progress <%= patient.wait_status_class %>">
              <span class="wait-time"><%= patient.wait_time_minutes %>m</span>
              <div class="progress-bar">
                <div class="progress-bar-fill" style="width: <%= [patient.wait_progress_percentage, 100].min %>%"></div>
              </div>
              <span class="wait-target"><%= patient.esi_target_label %></span>
            </div>
          </td>
          <td>
            <%= patient.chief_complaint %>
          </td>
          <td>
            <% if patient.rp_eligible %>
              <span class="rp-eligible">✓</span>
            <% else %>
              <span class="not-rp-eligible">✗</span>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
    </table>
  </div>
  
  <% if @patients.empty? %>
    <div class="empty-state">
      <p>No patients currently waiting</p>
    </div>
  <% end %>
</div>

<turbo-frame id="patient_modal"></turbo-frame>